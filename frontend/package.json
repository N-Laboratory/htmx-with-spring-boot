{
  "scripts": {
    "format:fe": "prettier --write ./**/*.{html,ts}",
    "format:be": "prettier --write ../mybatis-with-three-tier-architecture/src/main/resources/templates/**/*.html",
    "test": "vitest",
    "lint": "eslint --ext \".js,.ts\" --ignore-path .gitignore .",
    "lint:fix": "npm run lint -- --fix",
    "start:fe": "npx serve",
    "start:be": "java -jar ../mybatis-with-three-tier-architecture/build/libs/mybatis.sample.jar",
    "start": "run-p start:fe start:be",
    "build:be": "cd ../mybatis-with-three-tier-architecture && gradlew bootJar"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.4.0",
    "@typescript-eslint/parser": "^7.4.0",
    "eslint-config-prettier": "^9.1.0",
    "happy-dom": "^14.3.9",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.5",
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "puppeteer": "^22.6.1"
  }
}
